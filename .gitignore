# ไม่เอา library ขึ้น (เพราะหนักมาก และทุกคนกด npm install เองได้)
node_modules/
# ไม่เอาไฟล์รูปทีอยู่ใน uploads
public/uploads/*

# ไม่เอาไฟล์ความลับขึ้น (เช่น รหัสผ่าน database)
.env

# ไม่เอาไฟล์ขยะของระบบ
.DS_Store
Thumbs.db