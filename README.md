# ProjectWeb
## Concept

เว็บไซต์/แอปพลิเคชันที่ทำหน้าที่เป็น **แพลตฟอร์มกลาง** สำหรับ

- เชื่อมต่อคนใน มจธ ที่ต้องการทำกิจกรรมเดียวกัน

## Target

- คนที่ต้องการหาเพื่อนช่วยกันติวในรายวิชาเดียวกัน
- คนที่เหงา อยากหาเพื่อนทำกิจกรรมด้วย
- คนที่มีความสนใจเรื่องเดียวกัน

## Features

1.สร้างบัญชีผู้ใช้ โดยที่ต้องมีการยืนยันตัวตน (เมลมอ และหน้าของผู้ใช้)

2.สร้างห้องที่สามารถจอยเข้ามารวมตัวกันทำกิจกรรทต่างๆร่วมกันได้

3.ระบบคะแนนความน่าเชื่อถือของ User

4.ห้องแยกเป็นหมวดหมู่ เช่น ห้องติว, ห้องนัดกิจกรรม, ห้องนัดกินข้าว, ห้องนัดเล่นเกม

5.เขียนบทความลงหน้าเว็บได้

**6.ระบบ Matching/Recommendation**

- เช่น แนะนำเพื่อน/กลุ่มที่น่าจะเข้ากับผู้ใช้ จาก **วิชาที่เรียนเหมือนกัน / tag ความสนใจ / เวลาเรียนว่างตรงกัน**
- ทำให้ดูไม่ใช่แค่ CRUD แต่มีการใช้ **query ที่ซับซ้อน + algorithm เล็กๆ**

7.**ระบบ Notification**

- แจ้งเตือนถ้ามีคนกด join ห้องเรา, มีคอมเมนต์ใหม่ หรือ event ที่ใกล้จะถึง
- อาจเริ่มจาก **notification บนเว็บ** (ไม่ต้องถึง push จริงๆ) ก็เพิ่มความสมจริงได้เยอะ

8.**Gamification (Badge/Level)**

- เช่น คนที่เข้ากิจกรรมบ่อย ได้ badge "Active Member"
- หรือถ้ามีคนให้ rating ดี → ได้ “Trusted Member”
- จะทำให้ระบบน่าเล่นและดู advanced

**9.ระบบ Chat/Message ในห้อง**

- **แบบ real-time (ใช้ WebSocket / Firebase) หรือแบบง่ายคือข้อความในฐานข้อมูลแล้ว refresh**
    - **ทำให้เว็บดู interactive และท้าทายกว่าการเก็บข้อมูลเฉยๆ**

## ด้าน Database & CRUD

- ตารางหลัก: Users, Rooms, RoomCategories, Posts/Articles, Events, Messages, Ratings
- Feature CRUD:
    - User: สมัคร/แก้ไขโปรไฟล์/ลบ
    - Room: สร้าง/แก้ไข/เข้าร่วม/ลบ
    - Post/Article: เขียน/แก้ไข/ลบ
    - Event: สร้าง/Join/ยกเลิก
    - Rating: ให้คะแนน/คอมเมนต์

## UX/UI ที่เพิ่มคะแนนได้

- **Search + Filter**: เช่น ค้นหาห้องด้วยคีย์เวิร์ด, filter ตามหมวดหมู่, วันเวลา
- **Profile Page**: เห็นกิจกรรมที่เคย join, คะแนนความน่าเชื่อถือ, ความสนใจ
- **Dashboard**: สำหรับดูว่ามีกิจกรรม/ห้องที่ join ไว้วันไหนบ้าง

## ต้องลงอันนี้ก่อน
1. ลง Database ใน MySQL Workbench(MySQL) ใช้ไฟล์ `inint.sql` ในโฟล์เดอร์ database 
2. ลง npm และ extension : `npm install jsonwebtoken cookie-parser multer node-cron`
3. ลงโปรแกรม XAMPP ก่อน
4. ก็อป .env.example มาอีกไฟล์แล้วเปลี่ยนนามสกุลเป็น .env
5. ตั้งค่าตัวแปร และ Port ที่จะใช้ใน .env

## วิธี Run
1. Run `server.js` ก่อน
2. เปิด Browser แล้วเข้าตาม `http://localhost:<PORT>/` ได้เลย

## Feed
พฤติกรรมการเรียงลำดับ: ตอนนี้ระบบทำงานแบบ "ดึงมา 20 ห้องตามเวลาก่อน -> แล้วค่อยเรียงตามความสนใจเฉพาะ 20 ห้องนั้น"

สิ่งที่เกิดขึ้น: สมมติคุณชอบ "ดนตรี" แต่กิจกรรมดนตรีทั้งหมดอยู่อีก 3 เดือนข้างหน้า (ซึ่งอยู่ในหน้า 10) -> หน้าแรกคุณก็จะไม่เห็นกิจกรรมดนตรีครับ คุณจะเห็นกิจกรรมเร็วๆ นี้ก่อน (ตาม Logic Pagination ของ SQL)

สรุป: สำหรับ Project นี้ Logic ปัจจุบันถือว่ายอมรับได้และดีแล้วครับ (เพราะเราควรเห็นกิจกรรมที่ใกล้ถึงเวลาก่อนเสมอ) ไม่ต้องแก้ Backend ครับ แค่แจ้งให้ทราบเฉยๆ ครับ

## สิ่งทีต้องเพิ่ม/ควรเพิ่ม
- [x] ฟิลเตอร์กรอง (มาชกำลังทำ)
- [x] กิจกรรมที่สร้าง (มาชกำลังทำ)
- [x] ประวัติการเข้าร่วม (มาชกำลังทำ)
- [x] กิจกรรมที่เข้าร่วม (มาชกำลังทำ)
- [x] ลด Credit / กันไม่ให้เพิ่มเกิน 100 (มาชกำลังทำ)
- [x] แอดรูปกิจกรรมได้ (ภูกำลังทำ)
- [x] เจ้าของสามารถแก้ไขห้องที่สร้างเองได้ (ภูกำลังทำ)
- [x] แอดมินสามารถแก้ไขได้ทุกห้อง (ภูกำลังทำ)
- [x] การจอย (ภูกำลังทำ)
- [x] ออกจากห้อง (ภูกำลังทำ)
- [x] หน้าฟีดตามความสนใจผู้ใช้นั้นๆ (ภูกำลังทำ)
- [x] เลื่อนลงเพื่อโหลดห้อง (ภูกำลังทำ)
- [x] การลบห้อง (ภูกำลังทำ)
- [x] การเช็คชื่อทั้งคนสร้าง,เข้าร่วม (ภูกำลังทำ)
- [x] ถ้ากดค้นหาให้ไม่ใช้การเรียงตาม Tage มาเป็นตามเวลาปกติ (ภูกำลังทำ)
- [x] จดจำฉัน (ภูกำลังทำ)
- [x] ไม่ให้เจ้าของแก้ไข/ลบห้องถ้ากิจกรรมเริ่มแล้ว (ภูกำลังทำ)
- [x] การแสดงผลของ Admin

## เงื่อนไขการเช็คชื่อ
- ไม่สามารถเปิดได้ถ้าเวลาปิดกิจกรรม - เวลาปัจจุบัน แล้วน้อยกว่าหรือเท่ากับ 10นาที
- ถ้าสร้างห้องต่ำกว่า 15 นาที ให้ปิดฟังก์ชันเช็คชื่อไปเลย
- ต้องอยู่ในเวลากิจกรรมถึงจะเปิดการเช็คชื่อได้

## ขอบเขตของงาน
- ไม่รองรับกิจกรรมที่ใช้เวลาเกิน 1วัน



---------------------------------------

จากการที่เราคุยกันมายาวนาน ผมได้ช่วยคุณในหลายส่วนที่ **"ลึก"** และ **"สำคัญ"** กว่าแค่การเช็คคำผิดครับ เพื่อให้ Slide ดูมีน้ำหนักและโชว์ว่าเราใช้ AI มาช่วยในเชิง **Engineering** จริงๆ แนะนำให้เพิ่มหัวข้อเหล่านี้เข้าไปครับ:

คุณสามารถเลือกข้อที่ชอบไปใส่เพิ่มได้เลยครับ:

### 1. ด้าน Database & System Design (สำคัญมาก 🌟)
* **Database Schema Optimization:** ช่วยตรวจสอบและปรับปรุงโครงสร้างตาราง (ER Diagram) ให้ถูกต้องตามหลัก Normalization (เช่น การแยก Date/Time, การทำ Foreign Key Constraints).
* **Indexing Strategy:** แนะนำการสร้าง **Index** ใน Database เพื่อให้ระบบค้นหาและกรองข้อมูล (Search & Filter) ได้รวดเร็วเมื่อข้อมูลมีจำนวนมาก.
* **Data Integrity:** แนะนำการใช้ `UNIQUE` และ `Composite Key` ในตาราง Junction (เช่น `ROOMTAGS`) เพื่อป้องกันข้อมูลซ้ำซ้อน.

### 2. ด้าน Backend & Logic (ดูโปรมาก 🚀)
* **Complex SQL Logic:** ช่วยเปลี่ยน Logic การเช็คเงื่อนไขที่ซับซ้อนจาก JavaScript ให้ไปทำใน SQL แทน (เช่น การเช็ค `HAS_CODE`, `IS_EXPIRED` ใน Check-in API) เพื่อความแม่นยำและลดบั๊ก.
* **Concurrency & Race Condition Handling:** ช่วยแก้ปัญหา **Cron Job** ตัดคะแนน โดยแนะนำให้ยุบเหลือ Query เดียว (Atomic Operation) เพื่อป้องกัน User โดนหักคะแนนซ้ำซ้อน.
* **Code Refactoring:** ช่วยยุบรวม API ที่ทำงานคล้ายกัน (Created & History) ให้เป็น API เดียว (`/my-activities`) เพื่อลดความซ้ำซ้อนของโค้ด (DRY Principle).

### 3. ด้าน Security (ความปลอดภัย 🛡️)
* **Security Best Practices:** ให้คำแนะนำเรื่องการเก็บ Session ผ่าน **HTTP Only Cookies** แทน LocalStorage เพื่อป้องกัน XSS Attack.
* **Password Hashing Support:** แจ้งเตือนเรื่องขนาดความจุของ `VARCHAR` ในการเก็บรหัสผ่าน (Hash) ไม่ให้สั้นเกินไป.

### 4. ด้าน Frontend & UX
* **Infinite Scroll Logic:** ช่วยแก้ปัญหา **Edge Case** ของระบบโหลดข้อมูลอัตโนมัติ (กรณีจอใหญ่กว่าข้อมูล) ให้ทำงานได้อย่างสมบูรณ์.
* **Dynamic Filtering:** ช่วยวางโครงสร้าง JavaScript สำหรับการส่งค่า Filter หลายตัวพร้อมกัน (Search, Date, Tags) ไปยัง Backend.

---

### 📝 ตัวอย่างการเขียนลงใน Slide (แบบสรุป)

ถ้าพื้นที่ใน Slide มีจำกัด แนะนำให้เขียนรวบยอดแบบนี้ครับ:

**Tools: Gemini**
* **Database Design:** ช่วยตรวจสอบ ER Diagram, Data Dictionary และแนะนำการทำ Indexing เพื่อเพิ่มประสิทธิภาพ
* **Backend Logic:** ช่วยตรวจสอบ SQL Query ที่ซับซ้อน และแก้ปัญหา Concurrency ในระบบตัดคะแนน (Cron Job)
* **Refactoring & Debugging:** ช่วยปรับปรุงโครงสร้าง Code (Refactor) ให้เป็นระเบียบ และช่วยแก้บั๊ก Logic สำคัญทั้งฝั่ง Frontend/Backend
* **Security Check:** ให้คำแนะนำเรื่องความปลอดภัยของข้อมูล (Cookies, Password Hashing)

ใส่แบบนี้ อาจารย์จะเห็นว่าคุณใช้ AI เป็น **"Co-Pilot"** หรือ **"Thought Partner"** ช่วยคิดในเชิงลึก ไม่ใช่แค่ถาม Syntax เฉยๆ ครับ! 👍